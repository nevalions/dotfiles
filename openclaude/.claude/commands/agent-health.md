---
description: "One-screen verdict on the agent's freshness and self-diagnostics state"
agent: "plan"
---

!`python3 ~/code/agent/scripts/agent-health.py`

Relay the verdict above verbatim. If a "Diag note (auto)" line is present, replace it with the single most important WARN/FAIL from the diagnostics report if a better one is obvious from the text; otherwise keep it. Add nothing else.
